apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: directory-service, namespace: sso-platform }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: directory-service }
  minReplicas: 3
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
