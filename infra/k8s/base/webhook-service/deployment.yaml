apiVersion: apps/v1
kind: Deployment
metadata: { name: webhook-service, namespace: sso-platform, labels: { app: webhook-service, tier: platform } }
spec:
  replicas: 2
  selector: { matchLabels: { app: webhook-service } }
  template:
    metadata: { labels: { app: webhook-service, tier: platform } }
    spec:
      containers:
        - name: webhook-service
          image: sso-platform/webhook-serviceatest
          ports: [{ containerPort: 3300, name: http }]
          envFrom: [{ configMapRef: { name: sso-config } }]
          resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }
          livenessProbe: { httpGet: { path: /healthz, port: http }, initialDelaySeconds: 10 }
          readinessProbe: { httpGet: { path: /readyz, port: http }, initialDelaySeconds: 5 }
