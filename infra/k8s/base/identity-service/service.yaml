apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: sso-platform
spec:
  selector:
    app: identity-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: grpc
      port: 50052
      targetPort: 50052
