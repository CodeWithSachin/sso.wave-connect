apiVersion: v1
kind: Service
metadata: { name: authz-service, namespace: sso-platform }
spec:
  selector: { app: authz-service }
  ports:
    - { name: http, port: 8080, targetPort: 8080 }
    - { name: grpc, port: 50051, targetPort: 50051 }
