apiVersion: apps/v1
kind: Deployment
metadata: { name: login-portal, namespace: sso-platform, labels: { app: login-portal, tier: frontend } }
spec:
  replicas: 2
  selector: { matchLabels: { app: login-portal } }
  template:
    metadata: { labels: { app: login-portal, tier: frontend } }
    spec:
      containers:
        - name: login-portal
          image: sso-platform/login-portalatest
          ports: [{ containerPort: 80, name: http }]
          resources: { requests: { cpu: 50m, memory: 64Mi }, limits: { cpu: 200m, memory: 128Mi } }
          livenessProbe: { httpGet: { path: /, port: http }, initialDelaySeconds: 5 }
          readinessProbe: { httpGet: { path: /, port: http }, initialDelaySeconds: 3 }
